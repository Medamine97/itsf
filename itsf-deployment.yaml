# itsf-deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: itsf-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: itsf-service
  template:
    metadata:
      labels:
        app: itsf-service
    spec:
      containers:
      - name: itsf-service
        image: nginx:alpine
        ports:
        - containerPort: 80
        volumeMounts:
        - name: html-content
          mountPath: /usr/share/nginx/html
        securityContext:
          runAsUser: 101
          runAsGroup: 101
          allowPrivilegeEscalation: false
      volumes:
      - name: html-content
        configMap:
          name: itsf-html
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: itsf-html
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
      <title>HELLO ITSF</title>
    </head>
    <body>
      HELLO ITSF
    </body>
    </html>
---
apiVersion: v1
kind: Service
metadata:
  name: itsf-service
spec:
  selector:
    app: itsf-service
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80